# LLM hallucinations
Repository for my master's thesis about techniques for reducing hallucinations in LLM applications.

## Installation
For now: Install Python 3.14.0. Create a virtual environment with ```py -3.14 -m venv venv```. Then activate it with ```venv\Scripts\activate```. Finally, install requirements with ```pip install -r requirements.txt```.
